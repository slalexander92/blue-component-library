export default {
	title: 'Styleguide/Spacing',
	tags: ['autodocs'],
}

const DefaultTemplate = () => {
	return (`<base-spacing></base-spacing>`);
}

export const Default = DefaultTemplate.bind({});
Default.args = {}
